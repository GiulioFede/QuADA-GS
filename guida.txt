1) Clona il progetto sulla repository
    clone quadags...

2) Per evitare problemi di percorsi, esegui dentro QuADA-GS (cartella principale)
    pip install -e .

3) Compilare rasterizzatore

    cd utils/gs_utils
    python setup_gscuda_adaptive.py install


[INFERENZA]
1) scarica pesi del modello (TODO: metti su google drive). Supponiamo si trovi in PATH_CHECKPOINT
2) crea una cartella LR_DIR e inserisci tutte le immagini LR di cui vuoi fare INFERENZA
3) crea una cartella HR_DIR dove verranno salvate tutte le HR predette

python inference/evaluate_inference.py \
    --path_to_image_dataset LR_DIR_PATH \
    --results-dir HR_DIR_PATH \
    --load_checkpoint PATH_CHECKPOINT \
    --scale_to_evaluate s \ 
    --dmax dL0 dL1 dL2
